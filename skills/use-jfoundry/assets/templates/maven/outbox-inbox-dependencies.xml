<dependencies>
    <!-- Reliable external domain event publication. -->
    <dependency>
        <groupId>io.github.xfoundries</groupId>
        <artifactId>jfoundry-outbox-spring-boot-starter</artifactId>
    </dependency>

    <!-- MyBatis-Plus OutboxMessageStore adapter. -->
    <dependency>
        <groupId>io.github.xfoundries</groupId>
        <artifactId>jfoundry-outbox-mybatis-plus-spring-boot-starter</artifactId>
    </dependency>

    <!-- Idempotent message consumption. -->
    <dependency>
        <groupId>io.github.xfoundries</groupId>
        <artifactId>jfoundry-inbox-spring-boot-starter</artifactId>
    </dependency>

    <!-- MyBatis-Plus InboxMessageStore adapter. -->
    <dependency>
        <groupId>io.github.xfoundries</groupId>
        <artifactId>jfoundry-inbox-mybatis-plus-spring-boot-starter</artifactId>
    </dependency>
</dependencies>

